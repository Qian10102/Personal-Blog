# 发现与决策

## 需求
- 基于 Apple Design skill 全面优化全部页面：liquid glass UI、易用性，以及 HTML 与其他代码的可读性。
- 保持博客的内容、双语能力与既有页面路由。

## 研究发现
- 当前视觉基线已采用渐变背景、透明卡片和浮动导航；材质语言基本一致，但 `global.css` 被压成高密度长行，难以维护和审查。
- 主页、归档、搜索、文章、关于页与 404 均复用基础布局，但不少 Astro 模板仍有相邻标签挤在同一行的情况。
- 语言菜单已经具备键盘、焦点和点击外部关闭逻辑；后续改动应保留这些交互保障。
- 液态玻璃的 reduced motion / transparency / contrast 回退已经存在，但覆盖面与材质强度需要统一审查。
- 搜索、主题和语言的客户端逻辑集中在页面内脚本；可以通过小型、命名清晰的辅助函数提高可读性，避免改变静态站点架构。

## 技术决策
| 决策 | 理由 |
|------|------|
| 将全局 CSS 拆分为清晰的主题、基础、组件、页面和媒体查询区块 | 消除过长行，降低液态玻璃样式的维护成本 |
| 保持仅使用 transform / opacity 的装饰性动画 | 避免影响阅读、滚动与低性能设备的体验 |
| 保留原生 `<details>` 作为语言菜单状态容器 | 保留可靠的键盘与语义基础，再增强状态管理 |
| 将内容表面从半透明提升为可读的磨砂表面，导航与菜单使用更重的材质 | 减少移动端背景与文字的视觉竞争 |
| 将页面进入动效收敛为即时渲染 | 博客阅读、归档和导航都是高频操作，不应等待装饰性动效 |
| 在移动端显示单独的横向导航带 | 保证 Home / Articles / Search / About 始终可达 |

## 遇到的问题
| 问题 | 解决方案 |
|------|---------|
| 暂无 | 暂无 |

## 资源
- Apple Design skill：本次界面、动效、可访问性与材质层级的工作准则。

## 视觉/浏览器发现
- 桌面首页：浮动半透明导航与英雄区域形成清晰的层次；当前材质在浅色大渐变上基本可读，但背景光斑和玻璃表面均较强，卡片列表需要更克制的层级以保持阅读节奏。
- 导航、语言与主题操作集中在右上角，点击目标尺寸合格；语言的三点提示在视觉上可识别为更多选项。
- 移动首页：英雄区的透明度与背景叠加导致正文、标题和行动链接对比度明显不足，这是首要修复项；首屏在 390px 宽度下仍有清晰的单列结构。
- 待检查文章页、搜索页和列表页的布局与交互。

## 实现结果
- 全站已落地清晰的三层材质：背景、内容容器和可操作浮层；透明度与模糊只用于区分层级，正文面板保持更实的对比度。
- 移动端英雄区域使用实色提升对比度，并移除内容入场动画，避免首次进入或滚动到页面底部时出现空白感。
- 移动端补回可见主导航，桌面端保留紧凑顶部导航；焦点态、跳至正文链接和横向可访问性均由共享样式覆盖。

## 验证发现
- 390px 首页渲染为可读的单列卡片，首屏标题与正文即时显示，顶部和移动导航均在首屏可见。
- 语言菜单展开时明确展示“中文”和“English”，选择中文后页面语言更新为 `zh-CN` 并自动关闭菜单。
- 主题切换后根元素主题与按钮 `aria-pressed` 同步为 dark / true，状态反馈正确。
- 1440px 首页中桌面导航可见、移动导航隐藏，主内容宽度为 1180px，阅读节奏和层次稳定。
- 文章页的目录使用具名标签关联；搜索 “Astro” 返回 1 条结果，归档页显示 4 张文章卡片和 3 个筛选入口。
